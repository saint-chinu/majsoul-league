# 完全自動更新の使い方

`run_auto_update.py` が、現行（新リーグ）シーズンの牌譜ID収集 → 牌譜バイナリ取得 →
サイト再生成 → GitHubへpush（→Pagesが自動デプロイ）までを**無人で**行う。
旧リーグのデータ（`admin_paifu_ids.csv` / `summary.csv` / 集計キャッシュ）には触れない。

## 初回セットアップ（1回だけ）

**かんたん版: GitHub Desktop で一度 Pull してから、`setup_auto_update.bat` を
ダブルクリックするだけ。** 最新化 → タスク登録（毎日5:00） → お試し実行まで自動で行う。
以下は手動でやる場合の手順。

1. いつも通り手動で一度ログインしておく（`browser-profile/` にセッションが残っていればOK）。
2. 動作確認: PowerShellで `py run_auto_update.py` を1回実行し、最後まで走ることを確認。
   - 初回実行時に `auto_update_config.json` が自動生成される。
3. タスクスケジューラ登録:
   - 「タスクの作成」→ 操作: `run_auto.bat`（開始オプションにこのフォルダを指定）
   - トリガー: 任意（例: 毎日 5:00）
   - **「ユーザーがログオンしているときのみ実行する」を選ぶこと**
     （ブラウザ表示とクリップボードを使うため。ログオフ中はスキップされる）
   - ログは `logs\auto_YYYYMMDD.log` に残る。

## シーズンが変わったら

`auto_update_config.json` の `new_season` を次の番号に上げるだけ。
（管理画面でシーズン切替クリックが必要な場合は `click_season_from_bottom` に
「下から何番目か」を設定する。0なら押さない。）

## 大会（リーグ）名が変わったら

ログイン直後に複数の大会が並ぶ「大会リスト」画面が出ることがあり、その場合は
`auto_update_config.json` の `league_name`（既定「テスト」）と同じ名前のカードを
自動で選ぶ。大会名を変えたときはこの値も変えること。既に大会の中に直接入る
URLの場合はこのカードが無いので何もせず素通りする。

## 安全設計（データを消さないための決まり）

- 牌譜ID CSVは**和集合でしか更新しない**。件数が減る書き込みは拒否して異常終了する。
- 取得0件・ログイン切れ・画面遷移失敗のときは**何も書かずに終了**する
  （中途半端な状態でサイトを作り直してpushしない）。
- 書き込み前に `backups/` へタイムスタンプ付きコピーを残す。
- 収集は削除ボタンに触れない多重ガード付き:
  - class/aria/アイコンに delete/削除 等の兆候があるボタンは押さない
  - 最初にUUIDが取れたボタンの列（x座標）で較正し、以後その列しか押さない
  - クリック後にモーダル/確認が開いたらEscapeで閉じ、そのボタンは二度と押さない
  - ネイティブ確認ダイアログは常に自動で「キャンセル」
- 牌譜バイナリ取得（`collect_records.py`）は取得済みをスキップするだけで、削除はしない。

## ログイン切れのとき

`LOGIN_EXPIRED` とログに出て、**何も更新せず**終了する。
手動で一度管理画面にログインし直せば、次回から再び自動で走る。

## 従来の手動フロー

`run_all.py` / `run_manual_season.py` はそのまま残してある（旧リーグの再集計や
シーズン一括取得はこちら）。自動更新と手動実行が同時に走らないようにだけ注意。
